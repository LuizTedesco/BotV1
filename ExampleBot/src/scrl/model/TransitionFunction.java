package scrl.model;

import org.jalt.model.action.Action;
import org.jalt.model.state.State;

public class TransitionFunction {
	public double getValue(final State pState, final State pFinalState, final Action pAction) {
		return 0;
	};
}
